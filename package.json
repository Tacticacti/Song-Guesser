{
  "name": "song-guesser",
  "private": true,
  "scripts": {
    "dev": "concurrently -n api,web -c yellow,cyan \"venv\\Scripts\\python -m uvicorn server:app --port 8000\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
